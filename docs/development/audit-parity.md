# Python audit parity tracker

Reference: local vault-security-audit Python implementation, inspected 2026-09-08. This is the completion checklist, not a description of already implemented behavior.

| Requirement | Evidence needed | State |
| --- | --- | --- |
| All 35 rules: POL-001..015, APPROLE-001..008, JWT-001/002/003/005/006, K8S-001..006, REF-001 | Equivalent findings on shared fixtures, negative cases and Vault-confirmed disputed semantics | Partial: 19 auth detectors compared against Python on 128 fixtures (284 findings); 9 policy detectors verified on 186 fixtures (102 findings) and HCL privilege signals connected; 6 relationship detectors compared against Python on 40 fixtures (74 findings); REF-001 compared on 16 assignment fixtures; all built-in detectors implemented, broader end-to-end semantics and collection coverage still pending |
| HCL parsing with source locations, attributes and parse diagnostics | Comment, wildcard, template and restriction fixtures | Literal ACL lexer/parser implemented with locations and explicit unsupported-expression errors; semantic literals normalized against Python fixtures |
| Policy privilege signals and escalation relationships | Cross-policy, role, entity and group fixtures | HCL policy signals feed auth detectors even when policy findings are disabled; POL-010..015 combine role assignments, token-role issuance and observed AppRole/Identity associations; nested group propagation implemented with provenance, cycle and missing-reference diagnostics; acyclic inheritance compared with Python on 8 fixtures; full alias coverage diagnostics pending |
| Collection: policies, mounts, auth roles, identity and aliases | Supported-source coverage and redaction checks | Partial: initial selected sources |
| Namespace recursion/filtering, policy/auth filters, limits, skip identity, source redaction | Collector integration fixtures | Object limits, policy/auth filters and skip Identity implemented; offline namespace isolation implemented; single-namespace collection implemented; recursive discovery and redaction pending |
| Bounded workers, rate limit, timeout, retries, max duration, progress | Fault-injection tests | Partial: bounded concurrent reads, request timeout, shared rate limiting and bounded retries; request timeout and total duration cancellation implemented; progress pending |
| Checkpoint/resume with max age and partial failures | Interrupted collection recovery test | Pending |
| Collect, scan, analyze, selective refresh into a new snapshot | CLI and UI workflows | Partial: collect/scan/analyze; selective refresh pending |
| SQLite import/compatibility with existing Python snapshots | Real schema fixture, counts, normalized findings | Pending |
| Offline snapshot diff | Added/removed/changed object and finding fixtures | Native CLI diff implemented with compatibility checks, resource/finding/Identity/coverage changes; UI comparison verified on synthetic snapshots; Python differential/export parity pending |
| Baseline create/apply and expiring exceptions | Suppression/fingerprint/expiry tests | Native baseline CLI create/apply verified; expiring exceptions implemented with date/scope validation; UI and Python baseline import pending |
| Profiles, thresholds, privileged-policy settings, severity/enabled overrides | Configuration validation and equivalent configured results | In progress |
| Rule catalog browse, YAML import, extension and configuration through UI | Browser create/edit/save/run workflow and immutable run revision | Implemented initial YAML catalog/editor/import, custom field detector, revisions; full detector parity pending |
| JSON and other Python export formats, fail-on and require-complete controls | CLI export/exit-code fixtures | Partial: JSON/JSONL/YAML and findings CSV exports; configurable fail-on threshold and completeness controls implemented; Python report directory/index parity pending |
| Persistent run history, filtering and access control | Browser/API tests | Initial implementation |
| Production build, native-only runtime, documentation and commits | Clean build, runtime demo, parity evidence | Initial implementation |

The Python implementation is a behavioral reference, not an infallible authorization oracle. Corrected semantics must be recorded and verified against Vault rather than preserving known false positives or false negatives. Existing request-audit logs and live management views remain separate.

Relationship checks currently follow the Python declaration-based grant model. They do not establish effective Vault authorization across competing ACL patterns, parameter constraints, required sudo, or successful authentication. Cross-block precedence and constrained-operation validation remain required before treating a reported chain as an executable escalation.

AppRole association collection reads RoleID endpoints and the entity-alias catalog, persisting only SHA-256 RoleID/alias-name hashes plus canonical entity IDs and auth mount accessors. Existing snapshots without these fields can only support inferred authentication steps. Full alias display/export remains pending.

REF-001 aggregates explicit, implicit-default and token allowed-policy references. It ignores allowed globs and disallowed policies, uses configured privileged-name patterns for severity, and suppresses absence findings when policy collection is incomplete. The built-in YAML limits REF-001 to AppRole/JWT/Kubernetes, as in the reference catalog; custom rules can reuse the detector for token roles. Native collection omits the synthetic root policy document, so the engine explicitly includes root in its known catalog.

Engine 9 derives Identity assignments separately from raw resources. New scans persist this analysis with the snapshot; offline analyze recomputes it and includes it in CLI JSON. The UI has a searchable assignment table with subject, policy, direct/inherited relationship and source group. Diamond inheritance is deduplicated by source group; cycles terminate and are reported as analysis gaps. Group policy inheritance is not automatically attributed to auth roles. Native tests cover nested groups, diamond paths, provenance, missing groups/entities and cycles; Acyclic inheritance matches Python on 8 fixtures covering both edge representations, diamonds and missing parents. Browser verification confirmed direct and inherited assignments with source group paths. The assignment table supports loading additional batches of 100 rows; large-table pagination still needs browser verification.

Offline comparison: `npm run audit -- diff OLD_RUN_ID NEW_RUN_ID` uses the selected SQLite database and VAULT_ADDR without a Vault token or network request. It rejects different targets, schemas, configuration fingerprints and engine versions. Exit codes are 0 for no added/changed high findings, 1 for added/changed high or critical findings, 2 for errors or incomplete analysis. Removed findings are labeled removed rather than resolved, with an explicit warning for collection/analysis gaps. Severity changes are retained even when evidence is identical (the Python fingerprint-only comparison can miss these). CLI smoke verification used two synthetic SQLite runs and confirmed exit 1 for a new high REF-001 finding.

The admin-authenticated GET /security-audit/diff endpoint uses the same compareRuns engine and target-scoped store lookups as the CLI. The web panel selects an old run, displays category counts, filters changes and expands before/after JSON. Browser verification on two synthetic runs showed one resource change and three added findings, with expanded evidence and Identity source-group provenance.

Baseline CLI: `npm run audit -- baseline-create RUN_ID FILE` writes a new 0600 JSON file (also valid YAML), refusing to overwrite an existing file. `scan --baseline FILE` and `analyze RUN_ID --baseline FILE` retain all findings and return separate fingerprint/status/gate records; known findings do not fail the severity gate. Collection/analysis gaps still return exit 2. Baselines require matching target, rule configuration fingerprint and engine version. Native fingerprints are versioned through the engine contract and are not claimed to match Python fingerprints. Missing baseline fingerprints are labeled absent, not verified remediated. Synthetic CLI verification confirmed high findings remain visible while a matching baseline produces exit 0 without a Vault token.

CLI `scan` and `analyze` accept `--exceptions FILE` using the Python YAML fields version/exceptions, id, rule_id, namespace, object_path, optional policy_path, owner, reason and expires. Expiry uses the local calendar date and remains active through the specified day. Rule IDs are exact; namespace/object/policy patterns use glob matching. Current native snapshots cover root only. Suppressed findings remain in the output with gate=false and full exception details; expired and unused entries are reported. Tests cover expiry boundary, path mismatch, invalid dates, duplicate IDs, unknown rules and required ownership. Controls UI, persistent revisions and cross-language controls fixtures remain pending.

CI arguments are now parsed strictly: unknown, repeated, missing-value, unsupported-command and conflicting options fail with exit 2. `scan`, `analyze` and `diff` accept `--fail-on none|critical|high|medium|low|info` (native default high), `--require-complete` (native default) and explicit `--allow-incomplete`. Findings at or above the threshold return 1; required completeness failures return 2; otherwise 0. These preserve the native CLI defaults/exit contract rather than the Python default none/exit-2 severity convention. Historical analyze validates exception rule IDs against the pinned catalog when available.

Offline exports: `npm run audit -- export RUN_ID FILE --format json|jsonl|yaml|csv`. Files are created with mode 0600 and exclusive creation. JSON/YAML preserve the stored detail, JSONL emits typed metadata/resources/findings/gaps/Identity assignments, and CSV exports findings with quoted cells and spreadsheet formula neutralization. These are native formats, not a claim of compatibility with the Python per-object YAML report tree and indexes. Export UI is implemented; Python report-layout parity remains pending.

The run page now offers report format selection and authenticated report download. API checks against the local synthetic database verified all four formats, three expected findings, attachment/no-store headers, HTTP 401 without authentication and HTTP 400 for unsupported formats. Browser inspection verified the controls and invoked JSON download without a page error; the browser-managed downloaded file was not independently inspected. Production build passed.

Collection now uses a shared serial request policy: default 10 requests/s, 3 retries, 500 ms exponential backoff capped at 10 seconds. Retryable HTTP statuses are 408/429/500/502/503/504; access errors and empty LIST responses are not retried. Existing VaultClient maps transport errors to 500. Snapshot collection metadata records parameters and request/retry/wait metrics. CLI scan accepts `--retries`, `--requests-per-second` and `--retry-backoff-ms`. Fake-clock tests verify spacing, eventual success, exhausted retries and immediate 403 failure. UI configuration, concurrent workers, total-duration bounds and live collector fault-injection coverage remain pending.

Collector parallelism: `scan --workers N` accepts 1..32 (default 10). Independent policy/Identity/role reads use a draining worker pool; discovery stages remain ordered. Every attempt passes a serialized admission queue for the shared rate limit, while network operations can overlap. Resources and issues are sorted before persistence. Local dev Vault comparison with workers=1 and workers=4 produced identical 12 resources, zero gaps and 13 requests. Tests verified peak concurrency and that active work settles before an error returns.

The run page exposes per-run workers, request rate, retries and initial backoff settings. POST /security-audit/runs validates numeric types/ranges and unknown fields before creating the run; the worker receives these validated options. Completed snapshot details display the recorded parameters and metrics. Unit validation and production build passed; the new settings form still requires browser end-to-end verification. Settings currently reset to defaults on page reload and are not a deployment-wide saved collection profile.

Duration limits: scan accepts `--timeout-ms` (default 30000) and `--max-duration-ms` (default 7200000), also exposed in the web collection form. The deadline AbortSignal cancels HTTP operations and rate/backoff timers; remaining reads do not start network requests. Already collected resources are returned with one collection/deadline gap and policiesComplete=false. A local raw HTTP fixture (supporting Vault LIST) stalled a policy read: the 200 ms deadline canceled it, retained the previously read policy and produced exactly one deadline gap. Unit tests and production build passed. The new duration controls still require browser verification.

`collect` now persists a read-only configuration snapshot with analysisPerformed=false and a distinct collected status, without running rules. It accepts collection/concurrency/deadline options and completeness controls. The UI explicitly labels the missing analysis, exports allow collected snapshots, and baseline/finding diff reject them. `analyze RUN_ID` evaluates the saved resources offline with the current configuration when no prior analysis is pinned. A temporary SQLite workflow against local dev Vault verified collect, stored status and offline analyze after removing VAULT_TOKEN; analysis produced expected POL-003 findings. Offline analyze prints by default; --save now persists a separate analyzed run.

Saved reanalysis: `analyze RUN_ID --save` persists a new result and records sourceRunId, retaining original collection timestamps and resources. `--current-rules` selects current rule settings; otherwise prior run settings remain pinned. The UI offers saved-snapshot analysis with current rules through the existing worker, without invoking collection. API checks source existence, finished state and target before creating the new run. A temporary SQLite CLI workflow without VAULT_TOKEN verified a separate completed result, unchanged collected source and retained collection timestamps. Browser/API end-to-end verification of the new reanalysis button remains pending; baseline/exception metadata is now persisted with CLI saved analyses.

Controls persistence: CLI scan and analyze --save store the applied baseline, exception definitions, evaluation date and per-finding states in the snapshot. Web analysis explicitly starts without inherited controls, avoiding stale suppression when replaying a source. Historical controls are shown as saved-date evidence; UI upload/edit/application implemented; browser validation pending. JSON/YAML preserve controls, JSONL metadata includes them, and CSV adds baseline_status/suppressed/gate/exception_id. SQLite round-trip verification confirmed immutable saved exception ownership, date and suppression after the input object changed.

Controls UI now accepts baseline YAML/JSON and exception YAML by editing or file import, creates a baseline from the selected analyzed run and applies the selected documents to either scan or reanalysis. POST validates both documents and baseline compatibility against a single pinned current catalog before creating the run. Local API verification created a baseline, rejected a different target with HTTP 400, and completed reanalysis retaining all three findings with baseline gates disabled. Production build passed. Full browser validation of file import/editor workflow and saved reusable controls profiles remains pending.

Object limits: `--max-objects N` and the web form count retained policies and subjects (roles/entities/groups/aliases), excluding mount metadata; 0 means unlimited. On the next object beyond the limit, active/queued requests are canceled and one collection/object-limit issue marks the saved snapshot incomplete. Native collection omits the synthetic root policy document. Diff rejects runs with different object limits. Local dev Vault verification with four workers and limit 2 retained exactly two counted objects and one limit gap, without mislabeling it as a deadline. Tests and production build passed; UI control browser verification remains pending.

Collection scope: CLI collect/scan accepts repeatable glob flags `--policy-filter`, `--auth-mount-filter` (mount without trailing slash), `--auth-type-filter`, plus `--skip-identity`. The collection API accepts arrays (up to 100 patterns) and a boolean skipIdentity. Scope is pinned in snapshot metadata and checked by diff; auth mount metadata remains available for Identity correlation even if role reads are filtered. Filtered policy catalogs disable absence checks; skipped Identity is an explicit analysis gap. Local dev Vault verification confirmed policy/auth-type filtering and no Identity resources or false REF-001 results. Repeatable CLI filters and web scope controls are implemented; browser verification remains pending.

Scope UI now supports newline-separated policy, auth-mount and auth-type patterns and a skip-Identity toggle. CLI repeated filter flags are collected, deduplicated and sorted while duplicate scalar flags remain errors. Tests verify repeated values, cross-command rejection and missing arguments; production build passed.

Engine 10 partitions offline resources by namespace before rule and Identity evaluation. Findings and inherited assignments retain namespace identity; policy completeness may be recorded per namespace. Fingerprints, exceptions and diff keys include namespace, and baseline/diff compatibility checks compare namespace sets. UI finding paths and Identity subjects show non-root namespace labels; CSV adds namespace. Synthetic tests confirmed that equal policy/role paths in two namespaces do not share privilege signals or policy references, and that exceptions remain scoped. Existing baselines require regeneration because engine/fingerprint semantics changed. Single-namespace collection now sends namespace request headers; recursive discovery remains pending.

Namespace collection: collect/scan accept `--namespace PATH`, falling back to VAULT_NAMESPACE when no explicit flag is provided; the web form has a namespace field. Requests send X-Vault-Namespace, resources retain namespace, and policy coverage is stored per namespace. Paths normalize boundary slashes and reject control characters, empty internal segments and dot traversal. A local raw HTTP fixture verified the header on every request and namespace/coverage metadata. This is transport verification, not an Enterprise Vault integration test. Recursive namespace discovery and scoped UI authentication remain to be addressed; existing server authentication still follows upstream VaultLens behavior.

Engine 11 reports Identity alias references to entities or auth mount accessors that were not collected, including missing reference fields. These remain coverage gaps: without explicit inventory completeness metadata, absence does not establish a stale alias. Namespace isolation applies before resolution, so an equal entity ID or accessor from another namespace cannot satisfy a reference. Tests cover resolved, missing, malformed and cross-namespace references, preserving raw resources. All 34 native tests and server type compilation passed. Embedded alias collection, catalog reconciliation and complete-inventory stale-alias classification remain pending. Existing baselines require regeneration for the new engine version.

Engine 12 collects embedded entity aliases with the same allowlist as standalone aliases: ID, canonical entity ID, mount accessor and SHA-256 name hash. Raw alias names and metadata are discarded. Per-namespace alias catalog completeness is recorded only after successful LIST/read collection without cancellation. Identity analysis reconciles embedded aliases by ID or reference/hash signature, reports differing catalog records without assuming an atomic snapshot, and reports absent entries only for an explicitly complete catalog. Unknown coverage on older snapshots does not establish absence. Diagnostics flow into existing UI/export analysis gaps. Tests cover redaction, signature fallback, inconsistent records and independent namespace coverage; all 35 native tests and server type compilation passed. Complete-auth-inventory stale-alias classification and browser presentation review remain pending.

Collector response validation now rejects a missing/non-object data envelope, malformed LIST keys and policies without string ACL source. Such responses produce explicit collection gaps and cannot establish complete policy/alias catalogs; no synthetic empty policy is retained. Vault LIST 404 remains the supported empty-collection response. A local raw HTTP fixture exercised malformed policy and alias catalogs, missing policy source and legitimate empty catalogs through the actual VaultClient and collector. All 36 native tests and server type compilation passed. This does not validate every auth-role response field or constitute Enterprise Vault integration coverage.

Cloud auth inventory now covers the Python ROLE_PATHS set: AWS, Azure, Alibaba Cloud and OCI use role; GCP uses roles. Collected records retain policy references and allowlisted common token settings. Missing Python safe fields (bound_cidrs, num_uses, local_secret_ids, user_claim, groups_claim, alias_name_source and allowed_entity_aliases) are retained as well. This is inventory coverage, not new provider-specific detectors. A raw HTTP collector fixture verified all five auth types, GCP's plural endpoint, common settings, no config endpoint reads and omission of injected credential fields. All 37 native tests and server type compilation passed. The checklist's previous auth-config wording was corrected: the inspected Python collector inventories mounts and roles, not separate auth/config endpoints. Actual provider integration remains unverified.

The audit UI now derives a policy usage index from saved resources and Identity assignments. It includes collected policies without references, explicit/implicit-default assignments, inherited references with source groups and token-role allowed-policy references labeled separately from assignment. Namespace keys remain isolated. Search covers namespace/policy/subject, a no-observed-references filter is available, and policy rows load in batches of 100. The UI explicitly excludes claims about unused policies because existing tokens, allowed globs and incomplete inventories are not resolved. A native test verifies namespace isolation, deduplication, provenance categories and immutable input. All 38 tests and production build passed; browser workflow verification and Python report/index export parity remain pending.

Browser verification on the updated local demo (engine 12): policy usage expanded the reader policy into its direct group and inherited entity references with source-group provenance; searching reader reduced three rows to one, and the no-observed-references filter excluded that assigned policy. A subsequent UI-triggered scan against localhost dev Vault used policy filter default, auth type token and skip Identity. The completed partial run recorded those exact options, five requests, eight resources, zero findings and two displayed coverage gaps (skipped Identity and filtered policy inventory). Its default policy remained visible when filtering for no observed references. This verifies the policy usage interaction and the filter/skip-Identity submission path, not Enterprise namespaces, every numeric collection option, file imports or large-list pagination. The demo server was refreshed to the current production build.

Report source redaction: `export RUN_ID FILE --redact-policy-source` removes full policy HCL from JSON/YAML/JSONL resources, retains a SHA-256 source digest and marks source_redacted=true. Matched ACL blocks and finding evidence remain intentionally visible, as explained by the UI checkbox. GET export accepts a strictly validated redactPolicySource=true|false option. CSV already contains findings only. Export works on a clone and does not change the stored snapshot or its replayability. Tests verify source omission, digest, evidence retention, immutable input and CLI scope; all 39 tests and production build passed. This covers report export only: Python's keep_policy_source=false SQLite storage behavior and the browser download workflow for this option remain pending.

Source-free storage is now available for CLI collect/scan via --redact-policy-source and the web collection checkbox. Collection retains source in memory for the initial scan; AuditStore removes HCL from a copied snapshot before SQLite serialization, preserving source hashes and the original findings (including matched blocks). CLI snapshot output follows the same option. The retained per-run option also applies to saved reanalysis. Offline replay explicitly reports unavailable policy source rather than treating it as an empty ACL. Existing snapshots are not retroactively rewritten. A temporary SQLite test verified initial policy findings, source omission, immutable in-memory input, persisted findings and replay gaps. All 40 tests and production build passed. Browser submission of the new storage option and checkpoint/resume behavior remain unverified; this mode does not remove evidence fragments or other metadata.

Diff now compares policy source by SHA-256 when a valid source digest is available, computing it from full HCL when retained. Changing retention mode alone does not create a policy change; changed hashes or missing hashes still remain visible. Original before/after records are preserved for actual changes. Coverage matching now includes namespace so identical issue paths from separate namespaces are not paired as changed issues. Tests cover full-source/source-free equality, changed/missing digests and namespace-separated gaps; all 41 native tests and server type compilation passed. This is content equality, not semantic ACL equivalence or trust validation for externally imported hashes.

Browser rule-extension workflow verified on the local demo: Append example populated YAML; an invalid object type was rejected without changing revision 0; a custom field_compare rule for acl_policy.name=default saved as revision 1 and appeared enabled in the catalog. UI saved-snapshot reanalysis produced its expected info finding and pinned revision 1, while the source run remained at revision 0 with no findings. Removing the demo rule saved revision 2 and restored the active catalog; historical output remains inspectable. Validation errors now list accepted object types, and the generic running text also fits offline reanalysis. Production build passed. This verifies editor/save/catalog/reanalysis, not file-upload import, concurrent edit conflicts or the complete configuration/profile matrix.

Initial Python SQLite import: `VAULT_ADDR=<matching address> npm run audit -- import-python snapshot.sqlite3` reads ordinary schema-3 tables using read-only SQLite with trusted_schema disabled, bounded file/row counts and validated policy digests. It saves a collected native snapshot for a separate `analyze RUN_ID --save`, retaining source scan ID, namespaces, policies, mounts, supported subjects, reconstructed assignment fields, alias hashes and coverage/warnings. Inherited assignments are recomputed from group metadata. No Vault token or network request is needed. Existing Python findings/configuration fingerprints/controls are not presented as native results. Source SQLite remains unchanged. A fixture generated through the Python write_snapshot function verified six resources, group inheritance, findings, alias-name redaction, target/schema rejection and missing-source replay diagnostics; all 42 tests passed, followed by server compilation after adding provenance. This is initial schema-3 configuration import: schema 2, full collection-option/coverage fidelity, Python control import, findings archival and web upload remain pending, so the import parity requirement is not complete.

Python import now retains normalized policy/auth filters, skip-Identity, max objects, selected collection sources, recursive-namespace intent and namespace filters in import provenance, plus original coverage records with counts/status/details. It does not invent native request metrics. Diff compares imported scope/limits/sources and rejects imported snapshots whose collection configuration is unknown, instead of silently assuming a full scan. Tests verify scope normalization, coverage counts, source mismatch rejection, missing scope rejection and invalid object limits; all 43 native tests and server type compilation passed. Scope metadata preservation does not yet establish full Python/native equivalence for every collection source or recursive discovery.

CLI import/reanalysis integration passed using a temporary Python fixture database and native destination with VAULT_TOKEN removed: import returned collected status with six resources; analyze --save --fail-on none created a distinct result with three findings, preserving source run and Python scan provenance. The UI now displays imported scan ID/timestamps, normalized collection scope and a searchable/paginated original coverage table. Unknown scope is explicitly labeled as unavailable for comparison. Production build passed; the new import detail panel still requires browser verification, and web upload remains pending.

Web SQLite import is implemented through the admin-authenticated POST /security-audit/imports/python endpoint (application/octet-stream, 32 MiB limit, SQLite signature validation). It writes a private temporary file, imports in the existing worker infrastructure, returns a separate run ID and removes the upload on worker exit. The UI accepts a file and selects the new run; analysis remains explicit. A dedicated local server/API integration imported the Python fixture as six resources with collected status, then reanalyzed it into a completed run with three findings; invalid file content returned HTTP 400. All 43 native tests and production build passed. File-picker/upload browser verification, upload-limit/auth failure integration checks and helpful worker failure reasons remain pending. CLI retains its larger 256 MiB import limit.

Failed runs now persist a safe failure reason in an additive SQLite column and display it in the UI. Python import failures distinguish target mismatch, unsupported schema, unfinished snapshot, policy digest mismatch and import limits; other errors use a generic data-integrity message without raw exception text. Worker exit callbacks cannot overwrite the stored reason or change a completed result. Tests verify persistence/listing, generic redaction and terminal-status preservation; all 44 native tests and production build passed. End-to-end browser rendering of failed imports remains pending.

Recursive namespace collection: collect/scan --recursive-namespaces and the UI checkbox discover child namespaces through sys/namespaces, then collect each with its own namespace header. Discovery and configuration reads share a global rate limiter, deadline and object budget. Per-namespace policy/alias coverage remains separate; failed child discovery is retained as a namespaced gap, malformed child paths are rejected, and discovery caps at 10,000 namespaces. Baseline namespace compatibility is checked against the actual collected set in the worker (API preflight cannot know the discovered tree). A raw HTTP fixture verified a three-level tree, per-namespace headers/resources, a 403 discovery gap, invalid child rejection and global object limiting. All 45 native tests and production build passed. Enterprise Vault integration, namespace filters and browser submission of the recursive option remain pending.

Namespace filters: repeatable --namespace-filter globs and the web form select namespaces after discovery; root matches the root namespace. Discovery traverses unselected ancestors to find matching descendants, while policy/Identity/auth reads only run in selected namespaces. Empty selection produces an explicit gap and incomplete policy coverage. Filter parameters are retained and checked by diff, including imported Python namespace filters. The raw HTTP fixture verified selecting only a grandchild, discovery without parent policy reads, empty selection and repeated CLI patterns. All 45 native tests and production build passed. Browser submission and Enterprise integration remain pending.

Background progress now persists current namespace, phase, resource count, request count and timestamp while a run is running. Collector stage/resource events feed the worker; updates are throttled to 250 ms within a stage and emitted immediately on stage changes. The existing UI poll shows this progress for collection, import and analysis. A late update cannot change terminal runs. Tests verify recursive namespace progress, final resource/request counters and SQLite terminal-state protection; all 46 native tests and production build passed. This is progress visibility, not resumable checkpoints; stalled requests may leave the last progress timestamp unchanged until completion/timeout. Browser verification remains pending.

Checkpoint persistence now saves partial snapshots between collection stages and after each processed namespace in CLI and worker runs. The checkpoint records its timestamp and processed namespaces, explicitly clears finishedAt/analysisPerformed and cannot establish globally complete policy coverage. SQLite recovery retains it with interrupted status; source-free storage applies to checkpoints as well. CLI analyze rejects unfinished/running/failed/interrupted snapshots, and the UI disables reanalysis while showing retained checkpoint details. Tests verify collector checkpoint events, persisted recovery, source removal, terminal write protection and export rejection; all 47 tests and production build passed. Resume/max-age handling is still pending, so checkpoint/resume parity is not complete.

CLI checkpoint resume: `resume RUN_ID [--checkpoint-max-age-ms N]` accepts failed/interrupted runs, validates matching target and original collection settings, and creates a separate analyzed run. Default maximum age is 24 hours (allowed range 1 ms to 7 days), measured from original collection start; future/invalid timestamps are rejected. Namespace discovery is refreshed; selected processed namespaces without issues or missing HCL are reused, while other namespaces are recollected. Resource limits include retained objects, request metrics accumulate across attempts, source timestamps and sourceRunId preserve provenance. A raw HTTP fixture verified root resource reuse without root policy requests and recollection of descendants; tests also cover setting mismatch, age/target rejection and source-free recollection. All 48 native tests and server compilation passed. Resume currently reuses whole namespaces, not individual successful source stages; web resume and real-process interruption integration remain pending.

Web resume is implemented for failed/interrupted checkpoints with a maximum-age input (1–168 hours), selected controls and current rule revision. POST /runs rejects mixed reanalysis/resume IDs or replacement collection options, validates age/target/status and restores saved options before creating a new worker. A separate fixture process saved one resource then stopped before finishing; a fresh server recovered it as interrupted, rejected a 1 ms age limit with HTTP 400, and resumed it through the API into a partial nine-resource result with retained skip-Identity scope and sourceRunId. The original checkpoint remained unchanged. Production build passed. This verifies process-exit recovery and API resume, not abrupt SIGKILL during a SQLite write or browser button interaction; per-stage reuse remains pending.

Resume now reuses successful source stages within an unfinished namespace: policies, Identity entities, groups, aliases, secret mounts and auth mounts/roles. A stage is recorded only after its work drains without new collection issues or cancellation; interrupted/failed stages are recollected. Policy and alias completeness is checkpointed at stage boundaries, and source-free policy stages are always refreshed. Legacy whole-namespace checkpoints remain supported. The HTTP fixture resumed a checkpoint taken after root policies but before Identity, verified no repeated policy request, observed subsequent Identity collection and retained correct policy completeness. All 48 native tests and server type compilation passed. Native stage boundaries differ from Python (auth mounts and roles share a stage); selective refresh and broader failure-injection coverage remain pending.

Selective CLI refresh: `refresh RUN_ID --source policies|identity|identity_aliases|mounts|auth_roles` (repeatable, default all) collects selected stages and saves a separately analyzed merged snapshot. collect/scan also accept --source. Stage results record success and completion time. A successful stage replaces old objects including confirmed absence; failed stages retain unmatched old objects while successful fresh reads replace matching records. Refresh retains source run, selected categories and the age/count of retained resources. Source issues are conservatively retained. Tests cover failed-stage retention versus complete-stage deletion. A local dev Vault CLI collect→policy-only refresh preserved all eight non-policy resources and left the source collected run unchanged. All 49 native tests and server compilation passed. Web refresh controls, finer scoped failure merging, explicit per-resource freshness and Python differential refresh fixtures remain pending; native mounts/auth roles share a collection stage.
